extends layouts/main-layout.pug

block styles
    link(rel="stylesheet" href="/css/list.css")

block content
    h1 List of Users
    if lists.length > 0
        ul.list
            each list in lists
                li #{list.name}
    else
        p No users found